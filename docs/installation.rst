============
Installation
============

At the command line::

    $ easy_install dj-beneficiary

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv dj-beneficiary
    $ pip install dj-beneficiary
